# Задача: документация в `docs/`

## Что хотел пользователь
Собрать пользовательскую документацию в каталоге `docs/`: навигация, Colab, карта репозитория, порядок запуска ноутбуков.

## Сделано
- `docs/README.md` — оглавление и быстрые ссылки на ноутбуки и скрипты пересборки.
- `docs/repository.md` — структура репозитория, таблица проектов с путями к `.ipynb`, граф идейных зависимостей.
- `docs/colab.md` — среда (T4), mock vs Drive, артефакты `run_*`, пересборка, inference-замечание про `train(mode=False)`.
- `docs/training_workflow.md` — рекомендуемый порядок 1→5, таблица сравнения метрик, пример кода агрегации.
- В [projects.md](../projects.md) добавлена одна строка-ссылка на `docs/README.md`.

## Проверенные пути (не дублировать проверку без причины)
- `docs/README.md`, `docs/repository.md`, `docs/colab.md`, `docs/training_workflow.md`
- `docs/superpowers/**` — не менялись (исторические планы/спеки)
- `tasks/_build_all_notebooks.py`, генераторы — не трогались в этой задаче

## Логика
Дублирование с `tasks/colab_notebooks_all_projects.md` минимизировано: в `tasks/` остаётся «журнал генерации и смоук», в `docs/` — пользовательский вход.
